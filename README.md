# webgoiysanpham

React + Vite + Ant Design starter with dark/light toggle, routing, and sample pages.

## Quickstart

```bash
npm install
npm run dev
```

Open http://localhost:5173

### Environment
Copy `.env.example` to `.env` and set `VITE_API_BASE_URL` to your backend.
